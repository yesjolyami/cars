document.addEventListener('DOMContentLoaded',()=>document.querySelectorAll('.accordion-trigger').forEach(button=>button.addEventListener('click',()=>{const panel=button.nextElementSibling,open=button.getAttribute('aria-expanded')==='true';document.querySelectorAll('.accordion-trigger[aria-expanded="true"]').forEach(other=>{if(other!==button){other.setAttribute('aria-expanded','false');other.nextElementSibling.classList.remove('is-open')}});button.setAttribute('aria-expanded',String(!open));panel.classList.toggle('is-open',!open)})));
